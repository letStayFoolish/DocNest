Create Frontend App with modern styling to display my .md files
The idea is to have all my .md files (documentation) in one place deployed (on Vercel, Netlify or somewhere else) with nice visibility so I could come to, whenever I need, and use docs I have wrote.
Tech:
TypeScript, React or Next.
Backend we do not need, I plan to store every single .md file locally within the frontend app, load them on starting app. And I would like to have kind of a navigation from where I could click on a file and jump to its page so I have it single-page showed.
For styling, you could chose plain CSS, Tailwid with Shadcn or even MatirialUI.
The idea is to keep it simple but nice and tidy. I want to have it modern-look like.

Prepare everything to store it on my github and deploy it to one of the free services, chose whatever fits it the most.
I do not need authentication/authorization. Just home page with list of documentation (which should be dynamic). That means every time we add new file I want it to be listed (added to the list). 

With this small, but very useful, app I do not need to think about Apple notes or google docs to store documentation and have them in some kind of a plain text styled, but have them in one place unify and nice looking.

About animation, I am open to ideas, so yeah you can add it.

If I miss something, please ask to qlarify while this is in planing mode. So plan first A-Z then jump in to writing code.